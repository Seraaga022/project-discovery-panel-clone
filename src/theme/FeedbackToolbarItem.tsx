import { createTheme } from "@mui/material";

const FeedbackToolbarItem = () => {
  return createTheme({
    palette: {
      info: {
        main: "#252526",
        light: "#252526",
        dark: "#252526",
      },
    },
  });
};

export default FeedbackToolbarItem;
